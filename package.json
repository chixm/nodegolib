{
  "name": "nodegolib",
  "version": "1.0.0",
  "description": "node with golang binary ",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "generate": "node scripts/generate.js",
    "build:dll": "cd include && go build -o goshared.dll -buildmode=c-shared",
    "build:addon": "npx node-gyp rebuild",
    "build": "npm run build:dll && npm run generate && npm run build:addon"
  },
  "repository": {
    "type": "git",
    "url": "github.com/chixm/nodegolib"
  },
  "keywords": [
    "node",
    "go",
    "lib"
  ],
  "author": "chixm",
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "global": "^4.4.0",
    "node": "^22.15.0",
    "node-addon-api": "^8.3.1",
    "node-gyp": "^11.2.0"
  },
  "jest": {
    "silent": false,
    "verbose": true,
    "testEnvironment": "node"
  }
}